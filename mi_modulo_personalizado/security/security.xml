<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_mi_modulo_personalizado_user" model="res.groups">
        <field name="name">User</field>
        <field name="category_id" ref="base.module_category_administration_records"/>
        <field name="comment">El usuario podrá leer y editar registros creados por otros usuarios.</field>
    </record>

    <record id="group_mi_modulo_personalizado_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="comment">El usuario tendrá acceso completo a la configuración del módulo, incluyendo crear, leer, editar y eliminar registros.</field>
        <field name="category_id" ref="base.module_category_administration_records"/>
        <field name="implied_ids" eval="[(4, ref('group_mi_modulo_personalizado_user'))]"/>
    </record>

</odoo>
